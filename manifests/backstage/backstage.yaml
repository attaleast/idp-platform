---
apiVersion: v1
kind: Namespace
metadata:
  name: developer-portal
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: backstage-config
  namespace: developer-portal
data:
  app-config.yaml: |
    app:
      baseUrl: https://portal.attalea.pw
      title: IDP Portal
    backend:
      baseUrl: https://portal.attalea.pw
      cors:
        origin: https://portal.attalea.pw
        credentials: true
      auth:
        keys:
          - secret: ${BACKSTAGE_AUTH_KEY}
    organization:
      name: IDP Team
    auth:
      environment: production
      providers:
        github:
          production:
            clientId: ${AUTH_GITHUB_CLIENT_ID}
            clientSecret: ${AUTH_GITHUB_CLIENT_SECRET}
            signIn:
              resolvers:
                - resolver: usernameMatchingUserEntityName
    techdocs:
      builder: 'local'
      generator:
        runIn: 'local'
      publisher:
        type: 'local'
    catalog:
      locations:
        - type: url
          target: https://github.com/attaleast/idp-platform/raw/main/manifests/backstage/catalog/users.yaml
          rules:
            - allow: [User, Group]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: developer-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backstage
  template:
    metadata:
      labels:
        app: backstage
    spec:
      containers:
        - name: backstage
          image: ghcr.io/attaleast/idp-backstage:latest
          command: ["node", "packages/backend"]
          ports:
            - containerPort: 7007
          env:
            - name: AUTH_GITHUB_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: backstage-secrets
                  key: AUTH_GITHUB_CLIENT_ID
            - name: AUTH_GITHUB_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: backstage-secrets
                  key: AUTH_GITHUB_CLIENT_SECRET
            - name: BACKSTAGE_AUTH_KEY
              valueFrom:
                secretKeyRef:
                  name: backstage-secrets
                  key: BACKSTAGE_AUTH_KEY
          volumeMounts:
            - name: config
              mountPath: /app/app-config.from-configmap.yaml
              subPath: app-config.yaml
          args:
            - "--config"
            - "/app/app-config.yaml"
            - "--config"
            - "/app/app-config.from-configmap.yaml"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: config
          configMap:
            name: backstage-config
---
apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: developer-portal
spec:
  selector:
    app: backstage
  ports:
    - port: 7007
      targetPort: 7007
